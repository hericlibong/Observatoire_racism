"""CLI runners orchestrating the V2 contextualization pipeline.

These modules expose the main entry points (`run_pilot_v2`,
`run_phase_c_lot_v2`, `run_incremental_session_v2`) used to run the
pipeline end-to-end against a single source file, a phase-C lot or the
incremental session flow.
"""
